# 对话问题总结

> 整理时间：2026-09-06
> 涵盖内容：面试拷问模式（grill）修复、GitHub 推送、技术栈学习指南保存等问题

---

## 问题一：面试拷问模式为什么会问简历里不存在的内容？

### 现象
开启「面试拷问」模式后，AI 面试官会询问用户简历中根本没有提到的技术（如 SpringBoot、Kafka、Dubbo 等），没有真正基于用户上传的简历来提问。

### 根因分析
排查后发现 4 个关键 Bug：

1. **前端未强制开启 RAG**：当 `grillMode=true` 但 `docMode=false` 时，前端发送 `use_rag=false`，导致后端完全跳过文档检索，面试官拿不到简历内容。
2. **简历内容被挤出检索结果**：`RAG_TOP_K=4` 太小，面经和公共简历模板容易把用户本人的简历片段挤掉。
3. **保底判断失效**：用 `r in my_resume_hits` 做对象身份判断，但 `ChunkRef` 重建后对象身份变了，导致保底逻辑不生效。
4. **提示词约束太弱**：`grill_role` 提示词没有明确禁止询问简历外技术，AI 容易自由发挥。

### 修复方案

| 修改文件 | 修改内容 |
|---------|---------|
| `frontend/src/views/Chat.vue` | grill 模式强制 `use_rag=true`，默认 `rag_category='interview'` |
| `backend/app/services/chat_service.py` | grill 模式 `top_k` 从 4 提到 8；按 `(doc_id, chunk_index)` 精确匹配保底至少 2 条简历；强化提示词规则 |
| `fix_quotes.py`（辅助脚本） | 修复提示词中内嵌英文双引号导致的语法错误，替换为中文直角引号「」 |

### 提示词强化要点（grill 模式）
- **信任背书**：第一条回复必须先说出从用户简历里看到的具体项目名/技能名，让用户当场核对。
- **首轮必按简历出题**：第一个问题只能从「我的简历」的项目经历或技能栈里深挖，绝不可以从面经题库抽题。
- **严禁问简历外技术**：不得提及任何不在用户简历里、也不在参考资料中的技术名词（如 SpringBoot、Kafka、Dubbo、Redis 集群、分布式事务、K8s 等）。

---

## 问题二：GitHub 推送失败

### 现象
执行 `git push origin main` 时报错，无法连接到 GitHub。

### 根因
Clash 代理（`http://127.0.0.1:7897`）未启动，但 git 配置了 `http.proxy` 和 `https.proxy` 走该代理，导致连接失败。

### 解决
启动 Clash 代理后重新推送，成功。

---

## 问题三：技术栈学习指南.md 为什么没有保存到 GitHub？

### 现象
`技术栈学习指南.md` 文件已生成，但在 GitHub 仓库里看不到。

### 根因
文件只生成在 Trae 工作区根目录（`AppData\Roaming\TRAE SOLO CN\...`），没有复制到项目目录 `C:\Users\33083\Desktop\大学生系统`，因此 git 没有跟踪到该文件。

### 解决
将文件复制到项目根目录，提交并推送到 GitHub（commit `7c69a50`）。

---

## 问题四：git 提交时报「Author identity unknown」

### 现象
`git commit` 时报错：`fatal: unable to auto-detect email address`。

### 根因
git 全局配置中的 `user.name` 和 `user.email` 解析异常。

### 解决
在项目本地重新设置身份：
```bash
git config --local user.name "33083"
git config --local user.email "3308317228@qq.com"
```

---

## 涉及的 commit 记录

| Commit | 说明 |
|--------|------|
| `3e47608` | fix(grill): 三大修复确保面试官真的看用户简历且不瞎问 |
| `7c69a50` | docs: 新增技术栈学习指南 |

---

## 后续验证建议
1. 重启后端服务，测试 grill 模式是否真正基于简历提问。
2. 确认 AI 首条回复是否包含简历中的具体项目名/技能名（信任背书）。
3. 确认 AI 不会询问简历外的技术名词。
