# 大学生学习与求职智能助手 —— 代码重点讲解文档

> 本项目是一个**基于大模型的 AI 对话 + 知识库问答（RAG）系统**，面向大学生提供学习辅导、简历优化、面试经验问答三大场景。本文档提炼代码中最值得讲解的核心点，方便演示与答辩。

---

## 一、项目总览

| 维度 | 说明 |
| --- | --- |
| 项目定位 | 学习 + 求职智能助手：RAG 知识库问答、多模态图片理解、联网搜索、技能/工具调用 |
| 后端 | Python + FastAPI + SQLAlchemy + LangChain |
| 前端 | Vue 3 + Vite + Element Plus + Pinia |
| 数据层 | Docker 容器：MySQL（业务数据）/ Redis（缓存）/ Chroma（向量库） |
| 大模型 | DeepSeek（文本 + 视觉），兼容 OpenAI / 通义千问，无 Key 时自动降级为 Echo 演示模式 |
| 入口 | 后端 `uvicorn app.main:app --port 8000`；前端 `npm run dev`（5173） |

---

## 二、系统架构

```
┌───────────────────────────── 前端 (Vue3, :5173) ─────────────────────────────┐
│  Chat.vue 聊天页 │ Documents.vue 文档库 │ Skills.vue 技能市场 │ Admin 后台    │
│  SSE fetch 流式解析  →  逐字渲染（reactive 实时刷新）                          │
└───────────────┬──────────────────────────────────────────────────────────────┘
                │ /api/v1  (Vite 代理转发)
┌───────────────▼──────────────────────────────────────────────────────────────┐
│                         后端 FastAPI (:8000)                                  │
│  main.py 全局限流中间件 → api/v1 路由 → services 业务层                         │
│   ├─ 认证鉴权: JWT(security.py) + 密码 bcrypt                                 │
│   ├─ chat_service: SSE 流式 / 多轮记忆 / 工具调用循环                          │
│   ├─ rag_service: Chroma 向量检索 + 相关度阈值过滤                             │
│   └─ document_service: 上传→解析→切片→向量化→入库                             │
└───────────┬─────────────────────┬──────────────────────┬─────────────────────┘
            │ SQLAlchemy          │ chromadb HTTP        │
   ┌────────▼────────┐   ┌────────▼────────┐   ┌─────────▼─────────┐
   │  MySQL 业务数据  │   │ Chroma 向量库    │   │  Redis（预留缓存）  │
   │ 用户/会话/消息/文档│   │ 用户集合+公共库   │   │                  │
   └─────────────────┘   └─────────────────┘   └───────────────────┘
```

**讲解要点**：前后端分离 + 容器化数据层；后端按 `api → service → core` 分层；向量检索与业务数据分离存储。

---

## 三、核心链路一：一条消息如何流式回答（重点）

用户点击发送 → 前端 SSE → 后端 RAG 检索 → LLM 流式生成 → 逐字渲染，全过程如下：

```
1. 前端 send()            创建 asstMsg（reactive）→ chatStream() 发起 fetch POST
2. 后端 chat_stream()     敏感词过滤 → 建/取会话 → 用户消息入库
3. RAG 检索               rag_search() 在用户库+公共库按分类检索 top_k，过滤低相关度
4. LLM 生成               llm.astream() 边生成边 yield "delta" 事件
   若启用工具             _stream_with_tools() 真流式 + Function Calling
5. 前端渲染               delta 事件 → asstMsg.content += → DOM 实时刷新
6. 收尾                   end 事件 → 助手消息入库 → message_count+2
```

### 3.1 后端 SSE 帧结构（[chat_service.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/services/chat_service.py)）

- `start`：携带 `conversation_id`、`refs`（引用片段）、`rag_ok`（检索状态）
- `delta`：每段文本增量 `{content}`
- `tool`：工具调用开始/结束（前端展示"🔧 调用中…"）
- `end`：`message_id`、总字符数、工具轨迹
- `error`：错误信息

```python
# 核心：StreamingResponse 返回异步生成器
gen = chat_service.chat_stream(current, req, db)
return StreamingResponse(
    gen,
    media_type="text/event-stream",
    headers={"Cache-Control": "no-cache", "X-Accel-Buffering": "no"},
)
```

### 3.2 前端 SSE 解析（[conversation.js](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/frontend/src/api/conversation.js)）

用 **fetch + ReadableStream**（而非 EventSource）实现，因为它需要 POST 带 token 和 body；按 `\n\n` 切帧，解析 `event:`/`data:` 后回调 `onEvent({event, payload})`，并支持 `AbortController` 手动停止。

### 3.3 前端流式渲染的关键坑（已修复）

**Vue 3 响应性陷阱**：若 `asstMsg` 是普通对象直接 `push` 进 `messages.value`，回调里改 `asstMsg.content` 改的是原始对象，**不会触发 DOM 刷新**，表现为"非流式、答案最后一次性蹦出"。

**修复**：用 `reactive()` 声明，保证每次 delta 都实时渲染：

```js
const asstMsg = reactive({ _key: 'a-' + Date.now(), content: '', refs: [], tools: [] })
// delta 事件
asstMsg.content += payload.content || ''
scrollToBottom()
```

---

## 四、核心链路二：RAG 知识库问答（重点）

### 4.1 文档入库（[document_service.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/services/document_service.py)）

```
上传 → 按扩展名解析文本（pdf/txt/md/pptx/doc/docx）
     → 按 RAG_CHUNK_SIZE=512 切片、重叠 64 字符
     → 本地 Embedding 模型 bge-small-zh-v1.5 向量化
     → 写入 Chroma（集合名 user_{id}_docs，按 category 元数据过滤）
     → 同时入库 MySQL（文档元信息 + 去重 MD5）
```

**讲解亮点**：多格式解析的容错设计——`.docx` 除段落/表格外，还会全量扫描 `w:t` XML 节点兜底（解决文本框内容丢失）；旧版二进制 `.doc` 走兼容解析。

### 4.2 检索 + 分类差异化（[rag_service.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/services/rag_service.py)）

- 同时检索**用户私有库 + 公共内置库**（所有登录用户共享）
- 按分类 `material / resume / interview` 用 `where={"category": ...}` 过滤
- **相关度阈值过滤**：Chroma 距离 > `RAG_MIN_SCORE=1.5` 的片段视为不相关直接丢弃
- 合并排序取 top_k

### 4.3 分类决定"角色"（prompt 差异化）

| 分类 | 角色 | 示例系统提示 |
| --- | --- | --- |
| 学习资料 material | 耐心学习辅导老师 | 讲解概念、总结要点、多举例 |
| 简历 resume | 资深简历优化顾问 | 指出问题、量化修改建议、不编造 |
| 面试经验 interview | 面试准备教练 | 梳理考点、模拟题、答题思路 |

> 讲解时可以演示：同一问题在不同分类下，回答的口吻和侧重明显不同。

---

## 五、核心链路三：工具调用 / Function Calling（重点）

- 技能/工具由数据库（用户启用的 skills）驱动，经 [skill_service.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/services/skill_service.py) 组装为 LangChain BaseTool 列表
- **注意点**：LLM 返回的 tool_calls 用的是 `BaseTool.name` 而非业务 `skill_key`，且工具名必须全局唯一（否则 DeepSeek 400）
- 支持联网搜索（博查 bocha_search，国内可用）、arXiv 论文查询、DuckDuckGo 兜底等
- 前端通过 SSE `tool` 事件实时展示"🔧 调用中… → ✓"

### 工具调用循环（[chat_service.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/services/chat_service.py) 的 `_stream_with_tools`）

```
llm.astream() 流式输出文本
  → 若模型发起 tool_calls
  → yield tool(start) 事件 → 执行工具 → yield tool(done) 事件
  → ToolMessage 喂回 LLM → 继续下一轮流式（最多 max_iter=4 轮）
```

---

## 六、多模态图片理解（进阶亮点）

- 用户可上传图片，后端存到 `/uploads/` 并可通过 URL 访问
- 检测 `image_url` 时自动切换**视觉模型** `deepseek-v4-flash-vision-exp`
- 本地 `/uploads/` 相对路径会转成 **base64 data URL**（DeepSeek 视觉接口要求）
- 视觉模型不绑定 tools（部分视觉模型不支持 tools 协议），走完整答案切片伪流式

---

## 七、安全与工程化（重点，易被提问）

### 7.1 认证（[security.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/core/security.py)）

- 密码：`sha256+base64 预哈希`（绕开 bcrypt 72 字节限制）→ `bcrypt` 哈希
- JWT：HS256，`sub/user_id/role/exp`；**生产环境禁用默认密钥**（config 里有 model_validator 强制校验）

### 7.2 限流防爆破（[ratelimit.py](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app/core/ratelimit.py)）

- 进程内固定窗口计数器（线程安全，可平滑替换 Redis 实现）
- 登录：**每账号 5 次 / 10 分钟**、**每 IP 20 次 / 10 分钟**；注册按 IP；全局中间件单 IP 300 次/分钟

### 7.3 其他

- **敏感词过滤**：注册、发消息前检查
- **CORS**：仅允许前端开发地址
- **历史上下文管理**：字符预算 6000，超限自动截断更早消息，并在系统提示中告知用户
- **前端**：token 存 `sessionStorage`（关闭即失效，防 XSS 窃取）；暗色模式；PWA 离线可用

---

## 八、前端工程结构（[frontend/src](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/frontend/src)）

```
src/
├─ main.js            # 入口：注册 Pinia/Router/ElementPlus(中文)/暗色模式/PWA
├─ router/index.js    # 路由 + 全局守卫（未登录→/login，非管理员禁入 admin）
├─ stores/user.js     # Pinia：token/用户名/角色，sessionStorage 持久化
├─ api/               # axios 封装 + SSE chatStream 解析
├─ components/
│  ├─ MarkdownContent.vue   # marked + highlight.js 渲染代码块高亮
│  └─ skills/               # 技能市场 / 我的技能
├─ layouts/MainLayout.vue   # 顶栏导航 + 暗色开关 + 移动端适配
└─ views/
   ├─ Chat.vue        # 聊天页（核心）：SSE 流式、工具可视化、引用下载、追问推荐
   ├─ Documents.vue   # 文档库：上传/分类/分页/搜索
   ├─ Profile.vue     # 个人资料
   ├─ Skills.vue      # 技能市场
   └─ Admin.vue       # 管理后台：用户管理 + 系统统计
```

---

## 九、后端目录导读（[backend/app](file:///C:/Users/33083/Desktop/%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%B3%BB%E7%BB%9F/backend/app)）

```
app/
├─ main.py            # FastAPI 入口：CORS、全局限流中间件、静态文件、路由挂载
├─ config.py          # pydantic-settings 配置中心（环境变量/.env 覆盖）
├─ database.py        # SQLAlchemy 引擎/会话（MySQL 或 sqlite 兜底）
├─ api/v1/            # 路由层：auth/conversations/documents/skills/uploads/users/health
├─ core/              # llm / embeddings / vectorstore / security / ratelimit / sensitive
├─ models/            # ORM：User / Conversation / ConversationMessage / Document / Skill
├─ schemas/           # Pydantic 请求/响应模型
└─ services/          # 业务层：chat / rag / document / skill / user
```

---

## 十、可扩展性与降级设计（讲解加分点）

1. **LLM 多提供商 + 无 Key 降级**：DeepSeek / OpenAI / 通义千问，都没配 Key 时自动切 `EchoChatModel`（演示模式仍能验证"上传→检索→流式"整条链路）
2. **向量库双模式**：Chroma HTTP 服务（Docker）优先，连不上自动退到嵌入式内存
3. **数据库双模式**：`DATABASE_URL` 可指向 sqlite，本地零依赖开发
4. **限流可平滑上生产**：内存实现接口与 Redis 一致，可替换
5. **公共知识库内置**：seed 脚本从 GitHub 拉取公开数据集（面经/简历/学习资料）索引到共享向量库，一键入库 3000+ 分块

---

## 十一、讲解常见问题（FAQ 速答）

| 问题 | 速答 |
| --- | --- |
| 为什么用 SSE 而不用 WebSocket？ | SSE 是单向服务端推送，天然支持 HTTP 流式，配合 fetch 可带 token/body，实现简单、自动重连 |
| 为什么用 reactive 修流式？ | Vue3 只有通过响应式代理改属性才会触发渲染，普通对象直接改不刷新 |
| 为什么 RAG 要过滤相关度？ | 低相关片段会误导模型编造答案，距离阈值 1.5 直接丢弃 |
| 用户文档如何隔离？ | 每个用户独立 Chroma 集合 `user_{id}_docs`；公共库独立集合 |
| 长对话怎么控制 token？ | 字符预算 6000 从新到旧截断，保留最近轮次，超限告知用户 |
| 工具名为什么要唯一？ | DeepSeek 对重复 tool 名返回 400，且 LLM 按 BaseTool.name 调用工具 |
| 视觉模型图片怎么传？ | 本地路径转 base64 data URL，按 OpenAI 兼容多模态 content 格式传入 |
