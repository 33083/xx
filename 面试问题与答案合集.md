# 面试问题与答案合集

> 基于本项目技术栈整理 · 2026-09-06
> 涵盖：Python、FastAPI、Vue3、MySQL、Redis、RAG/LangChain、Docker、工程化等

---

## 一、Python 基础与进阶

### Q1：Python 中 `__init__` 和 `__new__` 的区别？
**答：**
- `__new__` 是静态方法，负责**创建实例**（分配内存、返回实例对象），是实例化时第一个被调用的方法。
- `__init__` 负责**初始化实例**（给实例设置属性），在 `__new__` 返回实例后被调用。
- `__new__` 必须返回一个实例，`__init__` 不能返回值（返回 None）。
- 不可变类型（如 `int`、`str`）的子类化需要重写 `__new__`。

### Q2：Python 的 GIL 是什么？对多线程有什么影响？
**答：**
- GIL（Global Interpreter Lock）是 CPython 的全局解释器锁，同一时刻只允许一个线程执行 Python 字节码。
- **影响**：CPU 密集型任务无法通过多线程提速（反而可能因锁竞争变慢）；IO 密集型任务（网络请求、文件读写）多线程仍有效，因为 IO 等待时会释放 GIL。
- **解决方案**：CPU 密集型用多进程（`multiprocessing`）绕过 GIL；IO 密集型用 `asyncio` 协程或多线程。

### Q3：`*args` 和 `**kwargs` 的用法？
**答：**
- `*args`：收集多余的**位置参数**为一个元组。
- `**kwargs`：收集多余的**关键字参数**为一个字典。
- 定义函数时 `*` 必须在 `**` 之前；调用时可用 `*` 和 `**` 解包列表/字典传参。

### Q4：装饰器（Decorator）的原理和实现？
**答：**
- 装饰器本质是一个**接收函数作为参数并返回新函数**的高阶函数，利用 Python 函数是一等公民的特性。
- 实现方式：
  ```python
  def decorator(func):
      def wrapper(*args, **kwargs):
          # 前置逻辑
          result = func(*args, **kwargs)
          # 后置逻辑
          return result
      return wrapper

  @decorator
  def target():
      pass
  ```
- `@decorator` 等价于 `target = decorator(target)`。
- 保留原函数元信息用 `functools.wraps`。
- 带参数的装饰器需要多嵌套一层（三层函数）。

### Q5：Python 的垃圾回收机制？
**答：**
- **引用计数**（主）：每个对象维护 `ob_refcnt`，引用 +1，释放 -1，归零时回收。无法解决循环引用。
- **分代回收**（辅）：将对象分为 0、1、2 三代，新生代回收频率高，老年代低。定期扫描解决循环引用。
- **标记-清除**：分代回收的底层算法，标记可达对象，清除不可达对象。

### Q6：深拷贝和浅拷贝的区别？
**答：**
- **浅拷贝**：创建新对象，但内部元素仍是原对象的引用（`copy.copy()`、切片、`dict.copy()`）。
- **深拷贝**：递归复制所有层级，新对象与原对象完全独立（`copy.deepcopy()`）。
- 对于不可变对象（int、str、tuple），深浅拷贝都不会创建新对象（共享引用）。

### Q7：`is` 和 `==` 的区别？
**答：**
- `is` 比较的是**对象身份**（内存地址，即 `id()`）。
- `==` 比较的是**值是否相等**（调用 `__eq__` 方法）。
- 小整数（-5~256）和短字符串因 Python 缓存机制，`is` 和 `==` 都为 True；大整数或长字符串 `is` 可能为 False。

### Q8：迭代器（Iterator）和生成器（Generator）？
**答：**
- **可迭代对象（Iterable）**：实现了 `__iter__` 方法，如 list、dict、str。
- **迭代器（Iterator）**：实现了 `__iter__` 和 `__next__` 方法，是一次性的，遍历完即空。
- **生成器（Generator）**：用 `yield` 关键字的函数，调用时返回生成器对象，惰性求值，节省内存。`(x for x in range(10))` 是生成器表达式。

### Q9：Python 的 `with` 语句原理？
**答：**
- `with` 是**上下文管理器**的语法糖，确保资源自动释放。
- 上下文管理器需实现 `__enter__`（进入时执行，返回值赋给 `as` 变量）和 `__exit__`（退出时执行，可捕获异常）。
- `contextlib.contextmanager` 装饰器可用 `yield` 简化实现。

---

## 二、FastAPI 与 Web 后端

### Q10：FastAPI 的核心特性？
**答：**
- 基于类型提示的**自动数据校验**（Pydantic）和**自动文档生成**（Swagger UI / ReDoc）。
- 原生支持**异步**（async/await），基于 Starlette（ASGI），性能接近 Node.js 和 Go。
- **依赖注入**（Depends）系统，简洁地管理认证、数据库会话等。
- 自动生成 OpenAPI schema。

### Q11：FastAPI 中 `Depends` 依赖注入的作用？
**答：**
- 实现代码复用和关注点分离，如数据库会话、用户认证、配置等。
- 依赖可以是函数或类，FastAPI 自动解析参数。
- 支持依赖的嵌套、缓存（`use_cache=True`）、yield 依赖（用于资源清理，如 db session 的 close）。
- 本项目中用 `Depends(get_db)` 注入数据库会话，用 `Depends(get_current_user)` 做认证。

### Q12：Pydantic 的作用？v1 和 v2 有什么区别？
**答：**
- Pydantic 是数据校验库，用类型提示定义模型，自动校验、序列化、生成 JSON Schema。
- **v1 vs v2**：
  - v2 核心用 Rust 重写（pydantic-core），速度提升 5-50 倍。
  - v2 用 `model_validator` 替代 v1 的 `root_validator`，`field_validator` 替代 `validator`。
  - v2 用 `model_validate` 替代 `parse_obj`，`model_dump` 替代 `dict()`。
  - v2 校验器默认模式是 `after`（v1 是 `pre`）。

### Q13：什么是 ASGI？和 WSGI 的区别？
**答：**
- **WSGI**（Web Server Gateway Interface）：Python Web 标准接口，**同步**，一次只处理一个请求。代表：Flask、Django。
- **ASGI**（Asynchronous Server Gateway Interface）：WSGI 的超集，支持**异步**和长连接（WebSocket、SSE）。代表：FastAPI、Starlette。
- ASGI 应用是一个 `async def app(scope, receive, send)` 函数。

### Q14：RESTful API 设计原则？
**答：**
- 用 URL 表示资源（名词），用 HTTP 方法表示操作：
  - GET：查询资源
  - POST：创建资源
  - PUT/PATCH：更新资源
  - DELETE：删除资源
- 无状态、统一接口、分层系统、可缓存。
- 版本控制：URL 路径（`/api/v1/`）或 Header。
- 本项目接口如 `POST /api/v1/auth/login`、`GET /api/v1/documents`。

### Q15：什么是 CORS？如何解决跨域？
**答：**
- CORS（Cross-Origin Resource Sharing）：浏览器同源策略限制跨域请求。
- 解决方式：
  - 后端设置 `Access-Control-Allow-Origin` 等响应头。
  - FastAPI 用 `CORSMiddleware` 中间件配置。
  - 开发环境用 Vite 代理（`server.proxy`）转发请求避免跨域。

---

## 三、数据库（MySQL + SQLAlchemy）

### Q16：SQL 注入是什么？如何防范？
**答：**
- **SQL 注入**：用户输入被拼接到 SQL 语句中，导致执行恶意 SQL。
- **防范**：
  - 使用**参数化查询**（预编译语句），如 `SELECT * FROM users WHERE id = ?`。
  - ORM（如 SQLAlchemy）默认使用参数化查询，避免手写拼接 SQL。
  - 输入校验和过滤。
  - 最小权限原则。

### Q17：索引的作用和底层原理？
**答：**
- **作用**：加速数据检索，类似书的目录。
- **底层原理**：MySQL InnoDB 使用 **B+ 树**索引。
  - B+ 树非叶子节点只存键值和指针，叶子节点存数据，叶子节点通过双向链表连接，利于范围查询。
  - 聚簇索引：叶子节点存整行数据（主键索引）。
  - 非聚簇索引（二级索引）：叶子节点存主键值，需回表查询。
- **最左前缀原则**：联合索引 `(a, b, c)`，查询条件必须从最左列开始才能命中索引。

### Q18：事务的 ACID 特性？
**答：**
- **原子性（Atomicity）**：事务中的操作要么全部成功，要么全部回滚（通过 undo log 实现）。
- **一致性（Consistency）**：事务前后数据处于一致状态（业务约束不被破坏）。
- **隔离性（Isolation）**：并发事务互不干扰（通过锁和 MVCC 实现）。
- **持久性（Durability）**：事务提交后数据永久保存（通过 redo log 实现）。

### Q19：MySQL 的四种隔离级别？
**答：**
1. **读未提交（Read Uncommitted）**：可读到未提交数据，存在脏读。
2. **读已提交（Read Committed）**：只能读已提交数据，解决脏读，存在不可重复读。（Oracle、SQL Server 默认）
3. **可重复读（Repeatable Read）**：同一事务内多次读取结果一致，解决不可重复读，存在幻读。（MySQL InnoDB 默认，用 MVCC + 间隙锁基本解决幻读）
4. **串行化（Serializable）**：事务串行执行，解决所有问题，但性能差。

### Q20：什么是 MVCC？
**答：**
- MVCC（Multi-Version Concurrency Control，多版本并发控制）：通过保存数据的多个版本，实现读写不阻塞，提高并发性能。
- InnoDB 每行数据有隐藏字段：`trx_id`（最后修改事务 ID）、`roll_pointer`（指向 undo log 的版本链）。
- 快照读（普通 SELECT）通过 ReadView 判断可见版本，不加锁。
- 当前读（SELECT ... FOR UPDATE、INSERT、UPDATE、DELETE）加锁。

### Q21：SQLAlchemy 中 Session 的作用？
**答：**
- Session 是与数据库交互的工作单元，管理对象的生命周期和事务。
- 常用操作：`add()`、`commit()`、`rollback()`、`refresh()`、`scalars(select(...))`。
- 本项目用 `get_db` 依赖注入提供 Session，请求结束自动关闭。

### Q22：数据库连接池的作用？
**答：**
- 频繁创建/销毁数据库连接开销大，连接池预先创建一批连接复用。
- SQLAlchemy 通过 `create_engine(pool_size, max_overflow, pool_recycle)` 配置。
- 避免连接泄漏：确保使用后关闭（`with` 语句或依赖注入的 yield）。

---

## 四、Redis

### Q23：Redis 为什么快？
**答：**
1. **纯内存操作**：数据存在内存，读写速度极快。
2. **单线程模型**：避免多线程上下文切换和锁竞争（Redis 6.0 引入多线程处理网络 IO，但命令执行仍单线程）。
3. **IO 多路复用**：epoll/kqueue 处理大量并发连接。
4. **高效数据结构**：SDS、跳表、压缩列表等。

### Q24：Redis 的五种基本数据类型及使用场景？
**答：**
- **String**：缓存、计数器（INCR）、分布式锁（SETNX）、限流。
- **List**：消息队列（LPUSH/BRPOP）、最新列表。
- **Hash**：对象存储（用户信息）。
- **Set**：去重、共同好友、标签。
- **ZSet（有序集合）**：排行榜、延迟队列。

### Q25：Redis 持久化方式？
**答：**
- **RDB（快照）**：定时将内存数据快照写入磁盘（`save`/`bgsave`），文件紧凑，恢复快，但可能丢失最后一次快照后的数据。
- **AOF（追加日志）**：记录每条写命令，数据安全性高，文件较大，恢复较慢。可配置 `appendfsync`（always/everysec/no）。
- 混合持久化（Redis 4.0+）：RDB 做基础，AOF 记录增量，兼顾速度和安全。

### Q26：Redis 缓存三大问题？
**答：**
1. **缓存穿透**：查询不存在的数据，请求直接打到数据库。
   - 解决：布隆过滤器、缓存空值。
2. **缓存击穿**：热点 key 过期瞬间，大量请求打到数据库。
   - 解决：互斥锁、热点 key 永不过期（逻辑过期）。
3. **缓存雪崩**：大量 key 同时过期或 Redis 宕机，请求全打到数据库。
   - 解决：过期时间加随机值、Redis 集群、多级缓存、熔断降级。

### Q27：如何用 Redis 实现接口限流？
**答：**
- 本项目用**固定窗口计数**：
  ```python
  key = f"ratelimit:{account}:{window}"
  count = redis.incr(key)
  if count == 1:
      redis.expire(key, 60)  # 60秒窗口
  if count > limit:
      raise RateLimitError
  ```
- `INCR + EXPIRE` 保证原子性（虽非单条命令，但在 Redis 中快速连续执行）。
- 其他算法：滑动窗口、令牌桶、漏桶。

### Q28：Redis 分布式锁的实现？
**答：**
- 基本实现：`SET key value NX EX seconds`（原子设置 + 过期时间）。
- 释放锁：用 Lua 脚本判断 value 是否匹配后再删除，防止误删他人锁。
- 红锁（Redlock）：多节点加锁，多数派成功才算加锁成功。
- 注意：锁的续期问题（看门狗机制）。

---

## 五、Vue 3 前端

### Q29：Vue 3 的响应式原理？
**答：**
- Vue 3 用 **Proxy** 重写响应式系统（Vue 2 用 `Object.defineProperty`）。
- `reactive()` 用 Proxy 代理对象，拦截 get（收集依赖）、set（触发更新）。
- `ref()` 用于基本类型，内部用 `Object.defineProperty` 包装为带 `.value` 的对象。
- 优点：可监听数组索引和新增属性，性能更好。

### Q30：`ref` 和 `reactive` 的区别？
**答：**
- `ref`：用于基本类型（也可用于对象），访问需 `.value`，模板中自动解包。
- `reactive`：用于对象/数组，直接访问属性，不能解构（解构会丢失响应性）。
- 复杂对象建议用 `reactive`，简单值用 `ref`。

### Q31：Vue 3 组件通信方式？
**答：**
1. **props / emit**：父子组件通信。
2. **v-model**：双向绑定（语法糖）。
3. **provide / inject**：跨层级通信（祖先→后代）。
4. **Pinia**：全局状态管理（推荐）。
5. **事件总线 / mitt**：任意组件通信（Vue 3 移除了 `$on`，需第三方库）。

### Q32：Pinia 是什么？和 Vuex 的区别？
**答：**
- Pinia 是 Vue 官方推荐的状态管理库，Vuex 的继任者。
- 区别：
  - Pinia 没有 mutations，直接在 actions 中修改 state（更简洁）。
  - 完整 TypeScript 支持。
  - 支持多个 store，模块化更自然。
  - 体积更小，API 更简单。

### Q33：Vue Router 的两种模式？
**答：**
- **Hash 模式**：URL 带 `#`，如 `http://x.com/#/chat`，靠 `hashchange` 事件实现，兼容性好，不美观。
- **History 模式**：URL 正常路径，如 `http://x.com/chat`，靠 `pushState` API，需后端配置（所有路径返回 index.html，否则刷新 404）。

### Q34：v-if 和 v-show 的区别？
**答：**
- `v-if`：真正的条件渲染，条件为 false 时 DOM 节点不渲染（销毁/重建），切换开销大，初始开销小。
- `v-show`：通过 `display: none` 控制显示，始终渲染 DOM，初始开销大，切换开销小。
- 频繁切换用 `v-show`，条件很少改变用 `v-if`。

### Q35：Vue 的生命周期钩子？
**答：**
- **创建阶段**：`setup()`（Composition API）、`onBeforeMount`、`onMounted`
- **更新阶段**：`onBeforeUpdate`、`onUpdated`
- **卸载阶段**：`onBeforeUnmount`、`onUnmounted`
- 常用：`onMounted`（DOM 挂载后请求数据）、`onUnmounted`（清除定时器、事件监听）。

### Q36：Axios 拦截器的作用？
**答：**
- **请求拦截器**：在请求发出前统一处理，如添加 token、设置请求头、loading 等。
- **响应拦截器**：在响应到达 `.then` 前统一处理，如错误码判断、token 过期跳转登录、数据格式化。
- 本项目 `request.js` 中配置了请求拦截器加 Authorization，响应拦截器处理错误提示。

---

## 六、RAG / LangChain / 向量数据库

### Q37：什么是 RAG？为什么需要它？
**答：**
- RAG（Retrieval-Augmented Generation，检索增强生成）：先从知识库检索相关文档，再将其作为上下文喂给大模型生成回答。
- **需要的原因**：
  - 大模型有知识截止日期，不知道最新/私有数据。
  - 直接微调成本高、更新慢。
  - RAG 可随时更新知识库，成本低，可追溯来源。

### Q38：RAG 的完整流程？
**答：**
1. **文档处理**：上传文档 → 提取文本 → 切块（chunk）。
2. **向量化**：用 Embedding 模型将每个 chunk 转成向量，存入向量数据库。
3. **检索**：用户问题转向量 → 在向量库中做相似度搜索 → 返回 top_k 相关 chunk。
4. **生成**：将检索到的 chunk + 用户问题组装成 prompt → 调用大模型生成回答。

### Q39：什么是 Embedding？
**答：**
- Embedding（嵌入）：将文本映射为高维向量（如 384 维），语义相似的文本向量距离近。
- 本项目用 `BAAI/bge-small-zh-v1.5` 中文嵌入模型，384 维。
- 相似度计算常用**余弦相似度**或**欧氏距离**。

### Q40：向量数据库和传统数据库的区别？
**答：**
- 传统数据库：精确匹配（`WHERE id = 1`）、范围查询。
- 向量数据库：**相似度搜索**（近似最近邻 ANN），找最相似的向量。
- 向量数据库用 HNSW、IVF 等索引加速近似搜索。
- 本项目用 **Chroma**，开源、本地可跑。

### Q41：文档切分（chunking）的策略？
**答：**
- **目的**：长文档不能整个塞进大模型（token 限制、成本、检索精度）。
- **策略**：
  - 固定大小切分：按字数切（如 512 字）。
  - 重叠（overlap）：相邻 chunk 重叠部分字符（如 64 字），保证语义连贯。
  - 递归切分（RecursiveCharacterTextSplitter）：按段落→句子→字的层级递归切分。
  - 语义切分：按语义边界切（需要额外模型）。
- 本项目用 `chunk_size=512, chunk_overlap=64`。

### Q42：LangChain 的核心组件？
**答：**
- **ChatModel**：大语言模型封装（如 `ChatOpenAI`）。
- **PromptTemplate**：提示词模板。
- **Tool / Toolkit**：工具（函数调用），用 `@tool` 装饰器定义。
- **Agent**：让大模型自主决定调用哪个工具。
- **Retriever**：检索器，封装向量库的相似度搜索。
- **Chain**：将多个组件串联（如 LLMChain、RetrievalQA）。

### Q43：什么是 Function Calling（工具调用）？
**答：**
- Function Calling：大模型根据用户意图，主动决定调用外部工具（函数）并生成参数，程序执行工具后将结果返回模型。
- 流程：
  1. 定义工具的 JSON Schema（名称、参数）。
  2. 调用模型时传入工具列表。
  3. 模型返回 `tool_calls`（工具名 + 参数）。
  4. 程序执行工具，将结果作为 `tool` 消息返回模型。
  5. 模型基于工具结果生成最终回答。
- 本项目用 `llm.bind_tools()` 绑定计算器、搜索等工具。

### Q44：如何评估 RAG 效果？
**答：**
- **检索质量**：召回率（相关文档被检索到的比例）、精确率（检索结果中相关文档的比例）。
- **生成质量**：回答是否基于检索内容（无幻觉）、是否完整、是否流畅。
- **常用方法**：
  - 人工标注评测集。
  - 用 LLM-as-judge 自动评估。
  - 检索分数阈值过滤（本项目 `RAG_MIN_SCORE=1.5`）。

### Q45：如何防止大模型幻觉（Hallucination）？
**答：**
- 提示词约束：明确要求"只基于参考资料回答，不知道就说不知道"。
- 检索增强：提供真实上下文。
- 输出后验证：用检索结果交叉验证回答。
- 本项目 grill 模式提示词明确禁止询问简历外技术，并要求首条回复做信任背书。

---

## 七、Docker

### Q46：Docker 的核心概念？
**答：**
- **镜像（Image）**：只读模板，包含运行应用所需的代码、环境、依赖。
- **容器（Container）**：镜像的运行实例，隔离的进程。
- **仓库（Registry）**：存放镜像的地方（Docker Hub、私有仓库）。
- **Dockerfile**：构建镜像的脚本。
- **docker-compose.yml**：定义和运行多容器应用的配置文件。

### Q47：Docker 和虚拟机的区别？
**答：**
- **虚拟机**：每个 VM 有完整的操作系统，资源占用大，启动慢（分钟级）。
- **Docker**：容器共享宿主机内核，资源占用小，启动快（秒级），隔离性稍弱（Namespace + Cgroups）。

### Q48：Docker 的隔离原理？
**答：**
- **Namespace**：隔离进程、网络、文件系统、用户等（UTS、PID、Mount、Network、IPC、User）。
- **Cgroups**：限制资源使用（CPU、内存、磁盘 IO）。
- **UnionFS**：分层文件系统，镜像分层复用，节省空间。

### Q49：Docker Compose 常用命令？
**答：**
- `docker compose up -d`：后台启动所有服务。
- `docker compose down`：停止并删除容器。
- `docker compose ps`：查看服务状态。
- `docker compose logs -f`：查看日志。
- `docker compose exec <service> <cmd>`：进入容器执行命令。

---

## 八、网络与协议

### Q50：HTTP 和 HTTPS 的区别？
**答：**
- HTTP：明文传输，端口 80，不安全。
- HTTPS：HTTP + TLS/SSL 加密，端口 443，安全。
- HTTPS 握手过程：客户端发 ClientHello → 服务器返回证书 → 客户端验证证书 → 协商对称密钥 → 加密通信。
- 证书由 CA 机构签发，用于验证服务器身份。

### Q51：HTTP 常见状态码？
**答：**
- **2xx 成功**：200 OK、201 Created、204 No Content。
- **3xx 重定向**：301 永久重定向、302 临时重定向、304 Not Modified。
- **4xx 客户端错误**：400 Bad Request、401 Unauthorized、403 Forbidden、404 Not Found、429 Too Many Requests。
- **5xx 服务端错误**：500 Internal Server Error、502 Bad Gateway、503 Service Unavailable。

### Q52：什么是 SSE？和 WebSocket 的区别？
**答：**
- **SSE（Server-Sent Events）**：服务器单向推送数据到浏览器，基于 HTTP，自动重连，适合服务端推送（如 AI 流式输出）。
- **WebSocket**：双向通信，基于独立协议（ws://），适合实时双向交互（如聊天室）。
- 本项目聊天接口用 SSE 实现流式输出（打字机效果）。

### Q53：TCP 三次握手和四次挥手？
**答：**
**三次握手（建立连接）：**
1. 客户端发 SYN（seq=x）→ 服务器。
2. 服务器回 SYN+ACK（seq=y, ack=x+1）→ 客户端。
3. 客户端发 ACK（ack=y+1）→ 服务器，连接建立。

**四次挥手（断开连接）：**
1. 客户端发 FIN → 服务器（我发完了）。
2. 服务器回 ACK → 客户端（知道了）。
3. 服务器发 FIN → 客户端（我也发完了）。
4. 客户端回 ACK → 服务器，等待 2MSL 后关闭。

---

## 九、安全与认证

### Q54：JWT 的结构和原理？
**答：**
- JWT（JSON Web Token）由三部分组成：`Header.Payload.Signature`。
  - **Header**：算法和类型（Base64 编码）。
  - **Payload**：声明信息（用户 ID、过期时间等，Base64 编码，不加密）。
  - **Signature**：用密钥对前两部分签名，防篡改。
- 流程：登录成功 → 服务器生成 JWT → 前端存储 → 每次请求带在 `Authorization: Bearer <token>` → 服务器验证签名。
- 优点：无状态、跨域友好；缺点：无法主动失效（需黑名单或短期 token + refresh token）。

### Q55：密码如何安全存储？
**答：**
- **不能存明文**，也不能用 MD5/SHA（可彩虹表破解）。
- 用**慢哈希算法**：bcrypt、scrypt、Argon2。
- bcrypt 自动加盐，计算慢（可调 cost factor），抗暴力破解。
- 本项目用 bcrypt 哈希密码。

### Q56：XSS 和 CSRF 攻击？
**答：**
- **XSS（跨站脚本攻击）**：注入恶意脚本到网页，窃取 cookie 等。
  - 防范：输入过滤、输出转义、CSP 策略、HttpOnly cookie。
- **CSRF（跨站请求伪造）**：诱导用户在已登录网站发起恶意请求。
  - 防范：CSRF Token、SameSite cookie、验证 Referer。
- 本项目用 JWT 存在 sessionStorage，请求带在 Header 中，天然防 CSRF。

---

## 十、工程化与其他

### Q57：Git 常用命令？
**答：**
- `git add <file>`：暂存文件。
- `git commit -m "msg"`：提交。
- `git push origin main`：推送到远程。
- `git pull origin main`：拉取并合并。
- `git log --oneline`：查看提交历史。
- `git branch`：查看分支。
- `git merge <branch>`：合并分支。
- `git reset --hard <commit>`：回退到指定提交。
- `git stash`：暂存当前修改。

### Q58：什么是 CI/CD？
**答：**
- **CI（持续集成）**：代码提交后自动构建、测试，确保不破坏现有功能。
- **CD（持续交付/部署）**：自动将通过测试的代码部署到生产环境。
- 常用工具：GitHub Actions、Jenkins、GitLab CI。

### Q59：什么是虚拟环境？为什么需要？
**答：**
- 虚拟环境隔离不同项目的 Python 依赖，避免版本冲突。
- Python 内置 `venv`：`python -m venv .venv` 创建，`.\.venv\Scripts\activate` 激活。
- 本项目后端用 `backend/.venv/`。

### Q60：如何排查接口报错？
**答：**
1. 看浏览器 F12 Network 面板：请求 URL、状态码、请求参数、响应内容。
2. 看后端日志：是否有异常堆栈。
3. 确认数据库/Redis/Chroma 服务是否正常（`docker compose ps`）。
4. 用 Postman 或 curl 直接调接口，排除前端问题。
5. 检查配置文件（端口、密钥、数据库连接）。

---

## 十一、项目相关高频问题

### Q61：介绍一下你的项目？
**答：**
这是一个面向大学生的学习与求职智能助手，主要功能：
- **知识库 RAG 问答**：上传学习资料/简历/面经，AI 基于文档回答问题。
- **面试拷问模式**：AI 扮演面试官，基于用户简历针对性提问。
- **联网搜索**：结合网络信息回答。
- **技能市场**：可扩展工具（计算器、代码执行等）。
- 技术栈：Vue3 + FastAPI + MySQL + Redis + Chroma + LangChain + DeepSeek。

### Q62：项目中遇到的最大难点？
**答：**
- **RAG 检索精度**：如何让 AI 真正基于用户简历提问，避免幻觉。
  - 解决：增大 top_k、保底简历内容、强化提示词约束。
- **流式输出**：用 SSE 实现打字机效果，前端用 Vue 响应式对象实时更新 DOM。
- **限流**：用 Redis 固定窗口防止登录爆破，Redis 不可用时降级到内存字典。

### Q63：为什么选 Chroma 而不是 Milvus/Pinecone？
**答：**
- Chroma 开源免费、本地可跑，部署简单（Docker 一行命令）。
- 适合中小型项目，API 简洁。
- Milvus 更适合大规模生产，部署复杂；Pinecone 是云服务需付费。
- 本项目数据量不大，Chroma 足够。

### Q64：项目如何保证用户数据隔离？
**答：**
- 每个用户有独立的向量库集合（`user_{id}_docs`），检索时按 `user_id` 隔离。
- 公共知识库（`shared_interview_knowledge`）所有用户共享。
- 数据库查询都带 `owner_id == user.id` 条件。
- 认证用 JWT，每个请求验证用户身份。

### Q65：如果让你优化这个项目，你会怎么做？
**答：**
1. **检索优化**：用重排序（rerank）模型提升检索精度，或用混合检索（向量 + 关键词）。
2. **缓存**：热门问题加 Redis 缓存，减少大模型调用。
3. **异步任务**：文档上传处理用 Celery 异步化，不阻塞请求。
4. **监控**：接入日志系统和性能监控。
5. **测试**：补充单元测试和集成测试。

---

> **最后建议**：面试时结合项目实际经历回答，每个问题尽量用"是什么 → 为什么 → 项目里怎么用"的结构，比死记硬背更有说服力。
