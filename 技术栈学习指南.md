# 大学生学习与求职智能助手 · 技术栈学习指南

> 写给完全没接触过这些技术的你。
> 每一项都讲清楚：**是什么 → 怎么来的 → 我们项目里为什么用它 → 怎么重新学会它**。
> 按从前到后的学习顺序排列，不是按目录结构。

***

## 第一部分：先搞懂整个项目长什么样

打开 `docker-compose.yml` 你会看到三个服务：

```
用户浏览器 (Vue3 前端)
      ↓ HTTP / SSE
FastAPI 后端 (Python)
      ├──→ MySQL    存用户/会话/文档记录
      ├──→ Redis    存限流计数 / 缓存
      └──→ Chroma   存文档的"向量"（RAG 检索用）
                ↓
         DeepSeek API（大模型）
```

一句话：**前端用 Vue3 做界面，后端用 FastAPI 处理逻辑，数据存 MySQL，限流用 Redis，文档检索用 Chroma 向量库，最后调 DeepSeek 大模型生成回答。**

***

## 第二部分：前端技术（你看到的界面）

### 1. HTML / CSS / JavaScript

**是什么**：网页的三块基石。HTML 决定"有什么"（按钮、输入框），CSS 决定"长什么样"（颜色、布局），JavaScript 决定"怎么动"（点击响应、数据请求）。

**怎么来的**：

- 1991 年 Tim Berners-Lee 发明 HTML，让网页能显示文字和图片

- 1996 年 CSS 出现，之前每个网页都要手写样式，乱成一锅粥

- 1995 年 Brendan Eich 用 10 天写出 JavaScript，让网页能"动起来"

**我们项目里**：前端页面最终都会被构建工具打包成这三样东西，浏览器只认它们。

**怎么学**：

1. MDN Web 文档（mozilla 的免费教程）刷一遍 HTML/CSS 基础
2. 在 freeCodeCamp 上跟着做 3 个小练习
3. 不用背标签，用到时查 MDN 就行

***

### 2. Vue 3

**是什么**：JavaScript 框架。原生 JS 写复杂界面非常痛苦（手动操作 DOM），Vue 让你用"数据驱动"的方式写——数据变了，界面自动变。

**怎么来的**：

- 2014 年 Google 员工尤雨溪（Evan You）业余时间做出来的，目标是"比 Angular 简单，比 React 灵活"

- 2020 年发布 Vue 3，重写了底层（用 TypeScript + Proxy），更快更小

- 现在 Vue 和 React 是前端两大主流，国内 Vue 占有率更高

**我们项目里**：`frontend/src/` 下所有 `.vue` 文件都是 Vue 组件。每个组件分三块：

```
<template>  HTML 结构
<script>    JS 逻辑
<style>     CSS 样式
```

**怎么学**：

1. 官网教程 <https://cn.vuejs.org/tutorial/> 交互式教程，2 小时过一遍
2. 重点理解：响应式（ref/reactive）、组件传值、v-if/v-for、事件绑定
3. 看 `frontend/src/views/Chat.vue` 这个文件，它是我们项目最复杂的页面

***

### 3. Element Plus

**是什么**：基于 Vue 3 的 UI 组件库。Vue 只给你"框架"，Element Plus 给你"现成的按钮、表格、弹窗、图标"，不用自己从零画。

**怎么来的**：

- 2016 年饿了么前端团队做出 Element UI（Vue 2 版本），成为国内最流行的 Vue 组件库

- 2021 年跟随 Vue 3 发布 Element Plus，重写了全部组件

**我们项目里**：所有 `<el-button>` `<el-input>` `<el-icon>` 标签都是 Element Plus 的组件。

**怎么学**：

- 官网 <https://element-plus.org/zh-CN/> 直接看组件示例，用到哪个查哪个

- 不用全学，项目里用到的组件不超过 15 个

***

### 4. Vite

**是什么**：前端构建工具。你写的 Vue 代码浏览器看不懂，Vite 帮你：① 开发时启动本地服务器（`localhost:5173`），改代码自动刷新；② 发布时把代码打包压缩成浏览器能识别的 HTML/CSS/JS。

**怎么来的**：

- 之前用 Webpack，启动慢（几十秒），改一行代码要等好几秒

- 2020 年尤雨溪又做了 Vite，利用浏览器原生 ES 模块，启动只要 1 秒

- "Vite" 是法语"快"的意思

**我们项目里**：`frontend/vite.config.js` 配置了开发代理（把 `/api` 请求转发到后端 8000 端口）。运行 `npm run dev` 就是启动 Vite。

**怎么学**：

- 不用专门学，知道 `npm run dev`（开发）和 `npm run build`（打包）两条命令就够了

***

### 5. Pinia

**是什么**：Vue 3 的状态管理库。多个组件之间要共享数据（比如"当前登录用户是谁"），用 Pinia 集中管理，不用一层层传值。

**怎么来的**：

- Vue 2 时代用 Vuex 管理状态，但 API 设计繁琐

- 2021 年 Vue 核心团队推出 Pinia，更简单，直接取代 Vuex

**我们项目里**：`frontend/src/stores/` 下的文件存全局状态（如用户登录信息）。

**怎么学**：

- 官网 <https://pinia.vuejs.org/zh/> 30 分钟过一遍

- 如果项目里 store 不复杂，看一两个文件就懂了

***

### 6. Vue Router

**是什么**：前端路由库。让单页应用（SPA）实现"点链接换页面但不刷新整个浏览器"的效果。

**怎么来的**：

- 传统网站每个页面都是独立 HTML 文件，跳转要整页刷新

- 2014 年随 Vue 生态一起出现，让单页应用也能有"URL → 页面"的映射

**我们项目里**：`frontend/src/router/` 定义了 `/login` → 登录页、`/chat` → 聊天页等映射。

**怎么学**：

- 官网 <https://router.vuejs.org/zh/> 看基础教程

- 理解 `router-link`（跳转）和 `route.params`（取参数）即可

***

### 7. Axios

**是什么**：HTTP 请求库。前端要和后端通信（发请求拿数据），浏览器自带的 `fetch` 用着别扭，Axios 封装得更好用。

**怎么来的**：

- 2014 年 Matt Zabriskie 做的，原本叫 `$ajax`，后来改名 axios

- 现在是 GitHub 上 star 最多的 HTTP 库

**我们项目里**：`frontend/src/api/` 下的文件用 axios 发请求，比如 `POST /api/v1/auth/login` 登录。

**怎么学**：

- 官网 <https://axios-http.com/zh/> 看一遍

- 重点：`get` / `post` 方法、请求拦截器、响应拦截器

***

### 8. Marked + Highlight.js

**是什么**：两个库组合使用。Marked 把 Markdown 文本转成 HTML，Highlight.js 给代码块加颜色高亮。

**怎么来的**：

- Markdown 是 2004 年 Aaron Swartz 和 John Gruber 发明的轻量标记语言（用 `#` 表示标题、`-` 表示列表）

- Marked 是 2011 年的 JS 库，把 Markdown 转成网页能显示的 HTML

- Highlight.js 是 2006 年的代码高亮库，支持 190+ 种语言

**我们项目里**：AI 回答的内容是 Markdown 格式（带代码块），前端用这两个库渲染成好看的样子。

**怎么学**：

- 不用专门学，项目里已经配好了，看 `Chat.vue` 里怎么调用就行

***

## 第三部分：后端技术（Python 服务端）

### 9. Python

**是什么**：编程语言。语法简洁，适合快速开发，是 AI/数据领域最流行的语言。

**怎么来的**：

- 1989 年 Guido van Rossum 圣诞节假期闲着无聊，想做一个"介于 C 和 Shell 之间的语言"

- 1991 年发布 Python 0.9，名字来自英国喜剧团体 Monty Python

- 现在 Python 3.13 是最新版，我们项目用 Python 3.12/3.13

**我们项目里**：后端全部用 Python 写，在 `backend/app/` 目录下。

**怎么学**：

1. 《Python Crash Course》或廖雪峰 Python 教程
2. 重点：变量/函数/类/模块/装饰器/异步（async/await）
3. 不用学太深，能看懂项目代码就行

***

### 10. FastAPI

**是什么**：Python 的 Web 框架。让你用很少的代码写出高性能的 HTTP 接口，自动生成 API 文档。

**怎么来的**：

- 2018 年 Sebastián Ramírez 开发，基于 Starlette（ASGI 框架）+ Pydantic

- 设计灵感来自 Flask（简洁）和 APIStar（类型提示），但比它们快得多

- 现在和 Django、Flask 并称 Python Web 三大框架

**我们项目里**：`backend/app/main.py` 创建 FastAPI 应用，`backend/app/api/` 下的文件定义接口（如 `/auth/login`、`/conversations/chat`）。

**怎么学**：

1. 官网教程 <https://fastapi.tiangolo.com/zh/> 中文版，跟着做一遍
2. 重点：路径操作装饰器（`@app.post`）、依赖注入（Depends）、Pydantic 模型
3. 看项目 `backend/app/api/auth.py` 和 `backend/app/api/conversation.py`

***

### 11. Uvicorn

**是什么**：ASGI 服务器。FastAPI 写出来的代码不能直接跑，需要一个"服务器程序"来接收 HTTP 请求转给它处理，Uvicorn 就是干这个的。

**怎么来的**：

- 2018 年 Tom Christie（Django REST Framework 作者）开发

- ASGI 是 ASynchronous Server Gateway Interface 的缩写，支持异步

**我们项目里**：启动后端就是 `uvicorn app.main:app --port 8000 --reload`，`--reload` 表示改代码自动重启。

**怎么学**：

- 不用专门学，知道启动命令就行

***

### 12. Pydantic

**是什么**：数据验证库。你定义一个"数据模型"（比如"用户注册必须传 username 和 password"），Pydantic 自动帮你校验传进来的数据对不对。

**怎么来的**：

- 2018 年 Samuel Colvin 开发，利用 Python 的类型提示做运行时校验

- 2023 年 Pydantic 2.0 用 Rust 重写了核心，速度提升 5-50 倍

**我们项目里**：`backend/app/schemas/` 下的文件都是 Pydantic 模型，定义了接口的请求/响应数据格式。

**怎么学**：

- 跟着 FastAPI 教程一起学就行，它们是配套的

***

### 13. SQLAlchemy

**是什么**：ORM（Object-Relational Mapping，对象关系映射）库。让你用 Python 对象操作数据库，不用写 SQL 语句。

**怎么来的**：

- 2005 年 Michael Bayer 开发，是 Python 最早也最成熟的 ORM

- 理念：把数据库表映射成 Python 类，表的一行 = 一个对象

**我们项目里**：`backend/app/models/` 下的 `.py` 文件定义了数据表（如 User、Conversation、Document），`backend/app/core/database.py` 创建了数据库连接。

**怎么学**：

1. 官网教程 <https://docs.sqlalchemy.org/> 跟着做
2. 重点：定义模型（Column/Integer/String）、查询（select/where/join）、会话（Session）
3. 看 `backend/app/models/user.py` 理解模型定义

***

### 14. MySQL

**是什么**：关系型数据库。用表格形式存储数据，支持 SQL 查询语言，是全球最流行的数据库。

**怎么来的**：

- 1995 年瑞典 MySQL AB 公司发布，名字来自创始人女儿的名字 "My"

- 2008 年被 Sun 收购，2010 年被 Oracle 收购

- 开源免费，LAMP 架构（Linux+Apache+MySQL+PHP）的基石

**我们项目里**：存用户账号、会话记录、文档元信息、技能配置等。Docker 容器名 `campus-mysql`，端口 3306，账号 root/campus123。

**怎么学**：

1. 装一个 MySQL，跟着《MySQL 必知必会》做练习
2. 重点：SELECT / WHERE / JOIN / GROUP BY / 索引
3. 用 Navicat 或 DBeaver 连数据库看表结构

***

### 15. PyJWT + bcrypt

**是什么**：两个安全相关的库。PyJWT 生成和验证"令牌"（登录后发给用户的身份凭证），bcrypt 给密码加密（不能存明文密码）。

**怎么来的**：

- JWT（JSON Web Token）是 2010 年的开放标准，让登录状态不依赖服务器内存

- bcrypt 是 1999 年的密码哈希算法，故意设计得很慢，防止暴力破解

**我们项目里**：

- 登录成功 → 后端用 PyJWT 生成 token 发给前端

- 前端每次请求带 token → 后端验证身份

- 注册时密码用 bcrypt 加密后存数据库

**怎么学**：

- 搜"JWT 原理图解"看一遍流程就懂了

- 不用深学库 API，项目里 `backend/app/core/security.py` 已经写好了

***

### 16. Redis

**是什么**：内存数据库。数据存在内存里（不是硬盘），读写极快，常用作缓存、计数器、限流。

**怎么来的**：

- 2009 年意大利人 Salvatore Sanfilippo 开发，名字是 REmote DIctionary Server

- 他做这个是因为 MySQL 太慢了，想搞一个全内存的数据库

**我们项目里**：用来做"限流"（防止有人疯狂登录爆破密码），记录每个账号/IP 登录次数，超过就拒绝。Docker 容器名 `campus-redis`，端口 6379。

**怎么学**：

1. 装一个 Redis，学 5 种数据类型：string/list/hash/set/zset
2. 重点：INCR（自增计数）、EXPIRE（过期时间）、SET NX（分布式锁）
3. 看 `backend/app/core/ratelimit.py` 理解限流实现

***

## 第四部分：AI / 大模型 / RAG 相关

### 17. LangChain

**是什么**：大模型应用开发框架。帮你把"调用大模型 → 处理上下文 → 接入工具 → 管理记忆"这些流程串起来，不用自己从头写。

**怎么来的**：

- 2022 年 10 月 Rob Harrison 和 Chase Smith 开发，ChatGPT 爆火后的产物

- 2023 年成为 GitHub 增长最快的项目之一

- 目标：让大模型能"做事"（不只聊天），比如查数据库、搜网页、调 API

**我们项目里**：

- `backend/app/services/chat_service.py` 用 LangChain 调用 DeepSeek 大模型

- `backend/app/services/skill_service.py` 用 LangChain 的 `@tool` 装饰器定义工具

- 用 `llm.bind_tools()` 让大模型能调用计算器、搜索等工具

**怎么学**：

1. 官网 <https://python.langchain.com/docs/> 跟着教程做
2. 重点：ChatModel、PromptTemplate、Tool、Agent
3. 理解"Function Calling"：大模型决定要不要调工具，调哪个工具

***

### 18. Chroma（向量数据库）

**是什么**：专门存"向量"的数据库。普通数据库存文字/数字，向量库存的是一串数字（如 `[0.12, -0.34, ...]`），能做"语义相似度搜索"。

**怎么来的**：

- 2022 年 Chroma 公司发布，是 AI 时代专门为 RAG 设计的向量库

- 和 Pinecone/Milvus/Qdrant 同类，但 Chroma 开源免费、本地就能跑

**我们项目里**：

- 存文档的向量：上传文档 → 文本切块 → 每块转成 384 维向量 → 存入 Chroma

- 检索时：用户问题转成向量 → 在 Chroma 里找最相似的几块 → 喂给大模型

- Docker 容器名 `campus-chroma`，端口 8001（外部）→ 8000（容器内）

**怎么学**：

1. 理解"向量"：把文字变成一串数字，相似的内容数字也接近
2. 理解"相似度搜索"：比较两串数字的距离，越近越相关
3. 官网 <https://docs.trychroma.com/> 看 Getting Started
4. 看 `backend/app/core/vectorstore.py` 理解封装

***

### 19. Embedding 模型（BAAI/bge-small-zh-v1.5）

**是什么**：把文字变成向量的 AI 模型。输入一句话，输出一串数字（向量），语义相近的句子向量也相近。

**怎么来的**：

- 2023 年北京智源研究院（BAAI）发布 BGE 系列（BAAI General Embedding）

- `bge-small-zh-v1.5` 是中文优化的小模型，384 维，CPU 就能跑，适合个人项目

**我们项目里**：`backend/app/core/embeddings.py` 加载这个模型，文档入库和检索时都用它把文字变成向量。

**怎么学**：

- 搜"词向量/Word Embedding 原理"理解概念

- 不用深学模型内部，知道"输入文字→输出向量"就行

***

### 20. DeepSeek 大模型

**是什么**：深度求索公司的大语言模型。和 ChatGPT 同类，能理解文字、生成回答、调用工具。我们项目用 `deepseek-chat` 做主力，`deepseek-v4-flash-vision-exp` 做图片理解。

**怎么来的**：

- 2023 年深度求索公司发布，主打"性价比高"（比 GPT-4 便宜很多）

- 2024-2025 年成为国内最流行的 API 大模型之一

- 支持 Function Calling（让模型调用工具），和 LangChain 配合好

**我们项目里**：`backend/app/config.py` 里配了 `DEEPSEEK_API_KEY`，后端调它的 API 生成聊天回答、面试拷问题目等。

**怎么学**：

1. 注册 <https://platform.deepseek.com/> 拿 API Key
2. 看 API 文档 <https://platform.deepseek.com/api-docs/>
3. 不用学模型内部，理解"prompt → response"的调用模式即可

***

### 21. RAG（Retrieval-Augmented Generation）

**是什么**：检索增强生成。大模型本身不知道你上传的简历内容，RAG 的思路是：**先检索出相关文档 → 塞进 prompt → 让大模型基于这些内容回答**。

**怎么来的**：

- 2020 年 Facebook AI Research 论文提出，解决大模型"不知道私有数据"的问题

- 现在是 AI 应用最主流的架构之一

**我们项目里的完整流程**：

```
1. 上传文档 → 提取文字 → 切成 512 字一块 → 每块转 384 维向量 → 存入 Chroma
2. 用户提问 → 问题转向量 → 在 Chroma 找最相似的 4 块 → 塞进 prompt → 调 DeepSeek
3. DeepSeek 基于这些片段生成回答
```

**怎么学**：

1. 搜"RAG 原理图解"理解流程
2. 看 `backend/app/services/rag_service.py` 理解检索逻辑
3. 看 `backend/app/services/document_service.py` 理解文档处理

***

### 22. 文本切分（RecursiveCharacterTextSplitter）

**是什么**：把长文档切成小块的工具。一篇 1 万字的文档不能整篇塞给大模型（token 限制 + 太贵），要切成 512 字一块。

**怎么来的**：

- LangChain 提供的切分器，递归地按"段落 → 句子 → 字"的层级切分

- 保持语义连贯，不会从一句话中间切断

**我们项目里**：`backend/app/services/document_service.py` 里的 `_chunk_text` 函数，配置 `chunk_size=512, chunk_overlap=64`（每块 512 字，相邻块重叠 64 字保证连贯）。

**怎么学**：

- LangChain 文档里 Text Splitters 章节

- 理解 chunk\_size / chunk\_overlap 两个参数即可

***

### 23. tiktoken

**是什么**：OpenAI 的分词计数库。大模型按"token"计费，不是按字数。一个中文字大约 1-2 个 token，`tiktoken` 帮你数文本有多少 token。

**我们项目里**：在管理对话上下文长度时用到，避免超出大模型的 token 上限。

**怎么学**：

- 不用专门学，知道 `tiktoken.count(text)` 数 token 就行

***

### 24. pypdf + python-pptx

**是什么**：两个文档解析库。pypdf 从 PDF 提取文字，python-pptx 从 PPT 提取文字。

**怎么来的**：

- pypdf 是 2022 年从 PyPDF2 继承而来的活跃维护版本

- python-pptx 是 2010 年 Steve Canny 开发的 PowerPoint 操作库

**我们项目里**：文档库支持上传 PDF/PPT，用这两个库提取文字后做 RAG 索引。

**怎么学**：

- 不用深学，用到时查 API 文档

***

## 第五部分：工程工具

### 25. Docker + Docker Compose

**是什么**：容器化工具。MySQL/Redis/Chroma 这些服务如果手动安装很麻烦（版本冲突、环境差异），Docker 把它们打包成"容器"，一条命令就能跑。

**怎么来的**：

- 2013 年 Solomon Hykes 在 dotCloud 公司开发，开源后引爆"容器革命"

- Docker Compose 是配套工具，用一个 `docker-compose.yml` 文件定义多个容器，一条 `docker compose up -d` 全部启动

**我们项目里**：`docker-compose.yml` 定义了三个容器：

- `campus-mysql`（MySQL 8.0）

- `campus-redis`（Redis 7）

- `campus-chroma`（ChromaDB 1.5.9）

**怎么学**：

1. 装一个 Docker Desktop（Windows）
2. 学 5 条命令：`up -d`（启动）、`down`（停止）、`ps`（看状态）、`logs`（看日志）、`exec`（进容器）
3. 不用学镜像构建，本项目只用现成镜像

***

### 26. Git

**是什么**：版本控制工具。记录代码每次改动，能随时回退、多人协作、发布版本。

**怎么来的**：

- 2005 年 Linus Torvalds（Linux 之父）用 10 天写出来的，因为之前用的工具商业公司不让他用了

- 现在 GitHub 上托管了全球最多代码

**我们项目里**：代码提交到 GitHub 仓库 `https://github.com/33083/xx.git`。

**怎么学**：

1. 装一个 Git，学 5 条命令：`add`（暂存）、`commit`（提交）、`push`（推送）、`pull`（拉取）、`log`（看历史）
2. 注册 GitHub 账号，把项目 push 上去练手

***

### 27. pip + venv（虚拟环境）

**是什么**：pip 是 Python 的包管理工具（类似 npm 之于 Node）。venv 创建"虚拟环境"，让每个项目的依赖互不干扰。

**怎么来的**：

- pip 从 2008 年开始随 Python 分发

- venv 从 Python 3.3 开始内置，取代了之前的 virtualenv

**我们项目里**：后端用 `backend/.venv/` 这个虚拟环境，`pip install -r requirements.txt` 安装所有依赖。

**怎么学**：

- 学 3 条命令：`python -m venv .venv`（创建）、`.\.venv\Scripts\activate`（激活）、`pip install xxx`（装包）

***

### 28. npm

**是什么**：Node Package Manager，Node.js 的包管理工具。前端的库都通过 npm 下载。

**怎么来的**：

- 2010 年 Isaac Schlufer 开发，从"管理 Node 后端包"起步

- 后来前端库也用它管理，现在是全球最大的软件注册中心

**我们项目里**：`frontend/` 下运行 `npm install`（装依赖）、`npm run dev`（开发）、`npm run build`（打包）。

**怎么学**：

- 不用专门学，知道三条命令就行

***

## 第六部分：通信协议

### 29. HTTP / REST API

**是什么**：前后端通信的规则。前端发 HTTP 请求（GET 拿数据 / POST 提交数据），后端返回 JSON 格式数据。REST 是一种 API 设计风格（用 URL 表示资源，用方法表示操作）。

**怎么来的**：

- HTTP 是 1991 年万维网的通信协议

- REST 是 2000 年 Roy Fielding 博士论文提出的架构风格

**我们项目里**：

- `POST /api/v1/auth/login` 登录

- `POST /api/v1/conversations/chat` 聊天

- `GET /api/v1/documents` 文档列表

- URL 里 `/api/v1` 是版本前缀

**怎么学**：

- 装一个 Postman 或用浏览器 F12 的 Network 面板观察请求

- 理解 GET（取）/ POST（建）/ PUT（改）/ DELETE（删）四种方法

***

### 30. SSE（Server-Sent Events）

**是什么**：服务器主动推消息给浏览器的技术。不像普通 HTTP 一问一答，SSE 让服务器能持续"吐"内容到浏览器，实现流式输出。

**怎么来的**：

- 2009 年 HTML5 标准的一部分

- 比 WebSocket 简单（单向推送，不用双向通信）

**我们项目里**：聊天接口用 SSE 实现流式输出——AI 每生成一个字就推到前端，用户看到"打字机"效果，不用等整段回答生成完才显示。

**怎么学**：

- 搜"SSE 流式输出 原理"

- 看 `frontend/src/api/conversation.js` 里的 SSE 处理逻辑

***

## 第七部分：建议的学习路径

### 第 1 周：基础

| 学习内容           | 目标          | 怎么验证学会了     |
| -------------- | ----------- | ----------- |
| HTML/CSS/JS 基础 | 能写一个静态网页    | 做一个"自我介绍"页面 |
| Python 基础      | 能写函数/类      | 写一个计算器小程序   |
| MySQL 基础       | 会 SELECT 查询 | 建一张表存数据并查出来 |

### 第 2 周：前端

| 学习内容         | 目标          |
| ------------ | ----------- |
| Vue 3 官方教程   | 理解响应式、组件、指令 |
| Element Plus | 用组件搭一个表单页面  |
| Vite + npm   | 跑起来项目前端     |

### 第 3 周：后端

| 学习内容               | 目标                 |
| ------------------ | ------------------ |
| FastAPI 教程         | 写一个 hello world 接口 |
| SQLAlchemy + MySQL | 建表、插入、查询           |
| PyJWT              | 实现登录发 token        |

### 第 4 周：AI/RAG

| 学习内容               | 目标             |
| ------------------ | -------------- |
| 注册 DeepSeek API    | 调一次 API 拿到回答   |
| 理解向量/Embedding     | 知道文字怎么变向量      |
| LangChain + Chroma | 跑通"上传文档→检索→回答" |
| RAG 完整流程           | 理解切块→向量→检索→生成  |

### 第 5 周：工程化

| 学习内容      | 目标                                     |
| --------- | -------------------------------------- |
| Docker 基础 | 用 docker compose 启动 MySQL/Redis/Chroma |
| Git 基础    | 把项目 push 到 GitHub                      |
| 整体串联      | 从前端到后端到数据库跑通一条链路                       |

***

## 附录：项目依赖版本清单

### 后端 Python 依赖（requirements.txt）

| 包名                       | 版本        | 用途                      |
| ------------------------ | --------- | ----------------------- |
| fastapi                  | 0.115.6   | Web 框架                  |
| uvicorn\[standard]       | 0.34.0    | ASGI 服务器                |
| python-multipart         | 0.0.20    | 文件上传支持                  |
| pydantic                 | 2.10.4    | 数据校验                    |
| pydantic-settings        | 2.7.0     | 配置管理                    |
| email-validator          | 2.2.0     | 邮箱格式校验                  |
| SQLAlchemy               | 2.0.36    | ORM                     |
| pymysql                  | 1.1.1     | MySQL 驱动                |
| cryptography             | 44.0.0    | 加密库（MySQL 认证用）          |
| PyJWT                    | 2.10.1    | JWT 令牌                  |
| bcrypt                   | 4.2.1     | 密码哈希                    |
| redis                    | 5.2.1     | Redis 客户端（限流/缓存）        |
| langchain                | 0.3.13    | LLM 应用框架                |
| langchain-community      | 0.3.13    | LangChain 社区工具          |
| langchain-openai         | 0.2.14    | OpenAI/DeepSeek 集成      |
| langchain-text-splitters | ≥0.3.0    | 文本切分                    |
| chromadb                 | ≥1.3,<1.6 | 向量数据库                   |
| numpy                    | 2.1.3     | 数值计算（向量运算）              |
| openai                   | 1.58.1    | OpenAI SDK（兼容 DeepSeek） |
| pypdf                    | 5.1.0     | PDF 文本提取                |
| python-pptx              | 1.0.2     | PPT 文本提取                |
| tiktoken                 | 0.8.0     | Token 计数                |
| ruff                     | 0.8.4     | 代码检查工具                  |

### 前端依赖（package.json）

| 包名                      | 版本       | 用途              |
| ----------------------- | -------- | --------------- |
| vue                     | ^3.5.13  | 前端框架            |
| element-plus            | ^2.9.1   | UI 组件库          |
| @element-plus/icons-vue | ^2.3.1   | Element Plus 图标 |
| vue-router              | ^4.5.2   | 前端路由            |
| pinia                   | ^2.3.0   | 状态管理            |
| axios                   | ^1.7.9   | HTTP 请求         |
| marked                  | ^18.0.11 | Markdown 渲染     |
| highlight.js            | ^11.12.0 | 代码高亮            |
| vite                    | ^6.0.5   | 构建工具            |
| @vitejs/plugin-vue      | ^5.2.1   | Vue 的 Vite 插件   |

### 基础设施（docker-compose.yml）

| 服务     | 镜像                    | 端口        | 用途    |
| ------ | --------------------- | --------- | ----- |
| MySQL  | mysql:8.0             | 3306      | 关系数据库 |
| Redis  | redis:7-alpine        | 6379      | 缓存/限流 |
| Chroma | chromadb/chroma:1.5.9 | 8001→8000 | 向量数据库 |

### 外部 API

| 服务                                         | 用途     |
| ------------------------------------------ | ------ |
| DeepSeek API（deepseek-chat）                | 大模型对话  |
| DeepSeek API（deepseek-v4-flash-vision-exp） | 图片理解   |
| BAAI/bge-small-zh-v1.5                     | 文本向量模型 |
| 博查 AI 搜索                                   | 联网搜索   |

***

> **最后一句**：不用把上面所有东西都学完才开始看项目。先跑起来项目（用一键启动脚本），打开浏览器点点点，遇到不懂的再回来查对应章节。**带着问题学，比按顺序学快 10 倍。**

