# ===== 后端依赖 =====
# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# 配置 / 校验
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# 数据库
SQLAlchemy==2.0.36
pymysql==1.1.1
cryptography==44.0.0

# 认证
PyJWT==2.10.1
bcrypt==4.2.1

# 缓存
redis==5.2.1

# 大模型 / LangChain / RAG
langchain==0.3.13
langchain-community==0.3.13
langchain-openai==0.2.14
# 注意：不再依赖 langchain-chroma（它硬限制 numpy<2，与 Py3.13 + numpy 2.x 生态冲突）
#       改为 app/core/vectorstore.py 内 ChromaVectorStore 薄兼容层（直接用原生 chromadb API）
langchain-text-splitters>=0.3.0,<0.4
# chromadb 1.5.x: cp39-abi3 win_amd64 wheel（内嵌 hnswlib，无需源码编译），
#                  且对 numpy 的约束为 >=1.22.5，与 numpy 2.x 完全兼容
chromadb>=1.3,<1.6
# numpy 固定，避免被解算器选到无 wheel 版本（Py3.13 Windows）
numpy==2.1.3
openai==1.58.1
pypdf==5.1.0
python-pptx==1.0.2
tiktoken==0.8.0

# 多模态 OCR（后续模块）
# rapidocr-onnxruntime==1.4.4

# 开发工具
ruff==0.8.4
